image: "node:latest"

stages:
- pages

pages:
  stage: pages
  script:
  - git clone https://github.com/hakimel/reveal.js.git
  - mkdir public
  - mv reveal.js/* public
  - mv ressources index.html public/
  - cd public
  - yarn install
  - yarn build
  artifacts:
    paths:
      - public/
  only:
    - master


